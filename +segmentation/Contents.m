% +segmentation  (package namespace: segmentation.*)
% General texture-segmentation grouping algorithm (content-similarity matrix, local/mutual/transitive/confidence/region grouping). Applies to any texture image.
%
% Call functions as segmentation.<function>. Requires vision-commons on the MATLAB path (see each project's setup.m).

